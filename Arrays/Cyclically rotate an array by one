class Compute {
    
    public void rotate(int arr[], int n)
    {
        int ar[]=new int[n];
        ar[0]=arr[n-1];
        for(int i=1;i<n;i++)
        {
            ar[i]=arr[i-1];
        }
        for(int i=0;i<n;i++)
        {
            arr[i]=ar[i];
        }
    }
}
